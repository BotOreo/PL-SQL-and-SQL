SELECT area, doc_name, CONCAT(CONCAT('RM', ' '), salpermon) "SALARY"
FROM doctor
WHERE area IN ('Neurology','Rehab')
ORDER BY area asc, salpermon asc;

SELECT DOC_ID, DOC_NAME, CONCAT(SUBSTR(DOC_NAME,1,3), DOC_ID) AS "USERNAME"
FROM DOCTOR;   